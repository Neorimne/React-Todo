import React from 'react';
import { ReactComponent as Logo } from './preloader.svg';

const Preloader = (props) => {
    return (
            <div>
                 <Logo />
            </div>
    )
};

export default Preloader;

